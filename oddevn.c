#include <stdio.h>
int main()
{
    int num;
    printf("enter no.-");
    scanf("%d", &num);
    if (num % 2 == 0)
        printf("even");
    else
        printf("odd");

    // printf("enter no.-");

    // printf("enter no.-");

    // printf("enter no.-");

    return 0;
}